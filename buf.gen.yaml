# buf.gen.yaml
version: v2
clean: true # Cleans the output directory before generating
plugins:
  # Using a local plugin
  - local: protoc-gen-go
    out: gen/go
    opt: paths=import
  
  # Using a local gRPC plugin
  - local: protoc-gen-go-grpc
    out: gen/go
    opt: paths=import